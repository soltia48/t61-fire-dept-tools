[package]
name = "t61-fire-dept-tools"
version = "0.1.0"
edition = "2024"

[lib]
name = "t61_fd"
path = "src/lib.rs"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock"] }
encoding_rs = "0.8"
serde_json = { version = "1", features = ["preserve_order"] }
